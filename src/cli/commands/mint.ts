import * as p from "@clack/prompts";

export async function mintCommand(_args: string[]) {
  // Placeholder: minting requires mint authority; wiring depends on clone strategy.
  p.log.info("mint command is scaffolded. We will wire SPL minting after clone flow is finalized.");
}

